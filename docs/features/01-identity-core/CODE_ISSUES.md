# F01 Frontend Code Review Issues

> Issue snapshot: rows below record what the independent reviewers found at
> review time. [FIXES.md](FIXES.md) is authoritative for resolved/partial/open
> disposition after later codegen passes.

| ID | Severity | Finding and evidence | Required resolution | Disposition |
|---|---|---|---|---|
| F01-CR-001 | Blocker | The current Java service is a JWT resource server and supplies no BFF login entry point. The frontend must not imply otherwise. | Implement and test an executable same-origin BFF/OIDC flow before setting `VITE_OIDC_LOGIN_PATH`. | Open external integration blocker; the SPA now has no fabricated default and visibly blocks sign-in when unconfigured. |
| F01-CR-002 | Blocker | Legacy imported requirements are immutable and the backend exposes only collection reads. | Remove the incompatible POST client and every create affordance. | Resolved in `FIXES.md`; compatibility UI is read-only. |
| F01-CR-003 | High | `safeReturnPath` accepted `/\\attacker.example`, which browser URL parsing can resolve cross-origin. | Reject raw/encoded/repeatedly encoded backslashes, controls and every non-origin-relative value; enforce the same invariant server-side. | Resolved in `FIXES.md`; regression coverage added. |
| F01-CR-004 | High | `LegacyQueryService.java:43-54` returns records from every membership by default, but `src/lib/data-hooks.ts:110-141` never supplies `organizationId` and no current organization selector/scope is present. This leaves F01-T11 incomplete and makes multi-organization presentation ambiguous. | Add an authoritative current-org scope from `/me`/canonical API, pass it to every scoped query, show it in the shell, and retain server enforcement. | Open. |
| F01-CR-005 | Medium | The new SPA types in `src/lib/data-hooks.ts:5-100` are handwritten and legacy responses are cast directly in lines 110-144. The Java compatibility controller returns arbitrary imported JSON (`LegacyQueryService.java:67-74`), so an import drift becomes a runtime UI failure. | Publish the compatibility schema in executable OpenAPI or runtime-validate it at the boundary; generate/derive client types where practical. | Open. |
| F01-CR-006 | Medium | The scope screen still presents nonfunctional governance controls: `src/routes/scope.tsx:79-85` offers “Freeze” and “Run optimizer” with no handlers and uses “Auto-finalizes” language. | Make the controls visibly read-only/preview-only, or wire them only to canonical guarded workflow actions once implemented. | Open; later improvement. |
| F01-CR-007 | Low | `npm audit` reports five high-severity development-tool findings through ESLint/minimatch. `npm audit --omit=dev` reports zero production vulnerabilities. | Plan and test the ESLint 10 major upgrade (the available automatic fix is breaking); do not force it as an unreviewed migration change. | Open; not a production-runtime blocker. |

## Backend review issues — 26 July 2026

| ID | Severity | Finding and exact evidence | Required resolution | Disposition |
|---|---|---|---|---|
| F01-BE-CR-001 | Blocker | Authorization stops at active organization membership. `TenantAuthorizationService.java:28-46` has no permission, role, project/object assignment, object-state, or separation-of-duties check. `Membership.roleCode` is an unconstrained string (`Membership.java:27`), and V1 contains no roles, permissions, role-permissions, or scoped-assignment tables (`V1__identity_core.sql:17-39`). This fails F01-T01/T05 and the Project-A-only case in `TEST_CASES.md:9`. | Implement canonical roles/permissions and effective-dated scope assignments; require declared permissions at controller/service methods and resolve organization/engagement/project/object scope before querying data. | Open; Phase 1 release blocker. |
| F01-BE-CR-002 | Blocker | The decoder validates timestamps and audience but not issuer: `SecurityConfig.java:43-53` builds directly from a JWKS URI and registers only `JwtTimestampValidator` plus the custom audience validator. `application.yml:18-26` has no issuer property. | Require an exact configured issuer and validate `iss`, signature/JWK, time and audience together; document claims, algorithms, key rotation and service-account rules. | Open; authentication blocker. |
| F01-BE-CR-003 | Blocker | The Phase 1 schema is materially incomplete. V1 ends its canonical model at organizations/users/memberships/engagements/projects/months (`V1__identity_core.sql:1-105`); F01-T01–T04 require RBAC assignments, config versions, teams/contacts, approval policies/actions/delegation, guarded month transitions, and append-only history (`TASKS.md:9-12`). | Add append-only Flyway migrations and executable services for the missing Phase 1 model; do not mark F01 complete based on the read catalog. | Open; scope blocker. |
| F01-BE-CR-004 | High | Disabled/unknown subjects are not consistently denied. `MembershipRepository.java:14-39` correctly filters inactive users/memberships/dates, but `CatalogQueryService.java:52-57` and `LegacyQueryService.java:41-53` turn an empty membership set into HTTP 200 with an empty list. `/me` instead returns 404 (`CatalogQueryService.java:46-49`). T-IAM-006 requires the next protected request to be denied. | Resolve an active application principal once, fail closed with a consistent 401/403 when no active user/membership exists, and cover every endpoint family. | Open. |
| F01-BE-CR-005 | High | Database least privilege is absent. `application.yml:4-7` uses one datasource identity for Flyway and runtime, V1 defines no database roles/grants, and `compose.yaml:4-9` uses the same known `vms/vms_local` credentials while exposing PostgreSQL on all host interfaces. | Separate migration owner and restricted runtime roles; remove production-capable defaults, use secrets, restrict local port binding, and test grants. | Open. |
| F01-BE-CR-006 | High | Direct object lookup reveals cross-tenant object existence. Engagement, project, and month services fetch by global ID before authorization (`CatalogQueryService.java:71-74`, `83-88`, `98-103`), so an existing foreign ID returns 403 while an unknown ID returns 404. | Use tenant-scoped repository queries and a uniform not-found response for inaccessible objects; audit denied probes without exposing existence. | Open. |
| F01-BE-CR-007 | High | `projects.parent_project_id` references any project ID without requiring the same engagement (`V1__identity_core.sql:69-83`). A row can create a cross-engagement/cross-tenant hierarchy. | Enforce same-engagement parentage using a composite key/constraint or guarded service plus database trigger/function with integration tests. | Open. |
| F01-BE-CR-008 | High | Organization status is not part of authorization. Membership queries filter user and membership state/date (`MembershipRepository.java:18-35`) but do not require `m.organization.status = 'ACTIVE'`; an active membership in an inactive/archived organization remains authoritative. | Add organization-status checks to scope resolution and define the exact suspended/archive behavior. | Open. |
| F01-BE-CR-009 | Medium | ProblemDetail is not the typed domain-error contract required by F01-T14. `ApiExceptionHandler.java:20-46` returns raw exception messages, has no stable code/correlation ID, and has no conflict, stale-version, or invalid-transition handlers. | Define stable problem types/codes and correlation IDs; sanitize validation details; add 409/invalid-transition/stale-version mappings and contract tests. | Open. |
| F01-BE-CR-010 | Medium | OpenAPI is not executable contract evidence. `VmsWorkflowApplication.java:16-21` supplies metadata only; there is no bearer security scheme, common error schema, build-time spec generation/validation, or generated-client compatibility check. The legacy `JsonNode` response is untyped (`LegacyController.java:26-31`). | Declare OAuth2/bearer security and response schemas, generate/validate the document during `verify`, and contract-test the frontend boundary. | Open. |
| F01-BE-CR-011 | Medium | `LegacyQueryService.java:41-53` aggregates every active membership when `organizationId` is omitted, rather than resolving one current organization scope as required by architecture override line 43 and F01-T11. | Require an explicit current organization or a separately named cross-org permission; avoid silently blending tenants. | Open. |
| F01-BE-CR-012 | Medium | No CORS policy exists, so the supported deployment is implicitly same-origin even though the frontend accepts a separate API base URL. CSRF is disabled at `SecurityConfig.java:26`, which is suitable only while the API remains stateless bearer-only. | Document/enforce same-origin or configure a strict origin allowlist. If a cookie/BFF session is added, enable CSRF protection before any mutation endpoint. | Open architecture decision. |
| F01-BE-CR-013 | Low | The SQL in `LegacyQueryService.java:56-63` concatenates a table identifier, but the value comes only from the immutable allowlist at lines 17-23 and organization IDs remain bound parameters. The controller path is also regex-constrained. | Retain both allowlists and add a regression test; do not accept arbitrary identifiers later. | No SQL-injection defect found in current code. |
| F01-BE-CR-014 | Low | `pom.xml` has no SBOM, dependency vulnerability, convergence, static-analysis, or coverage gate; `verify` therefore gives no dependency-risk verdict. `postgres:18-alpine` is also a floating tag (`compose.yaml:3`). | Add pinned/reviewed images and CI dependency/SBOM/static-analysis checks with an explicit triage policy. | Open hardening item. |
