# Historical Import Sample Templates

These CSVs are synthetic examples matching PRD 18. Replace all `example.invalid` addresses, organization/project codes, external references and sample URLs with configured tenant values.

Important:

- Run dry-run validation before commit.
- Import files in numbered order.
- Use either raw punches or daily attendance as the authority for a date range; do not add both.
- Upload referenced evidence files separately through the secure import/evidence flow.
- Do not add salary, rate, CTC or markup columns.
- These samples are not production evidence.
